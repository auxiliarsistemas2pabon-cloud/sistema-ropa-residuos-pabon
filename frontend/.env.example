# Dirección de la API de Django. Si se deja sin definir (recomendado en
# desarrollo), la aplicación usa el mismo equipo desde el que se abrió la
# página, en el puerto 8000: sirve igual con localhost, 127.0.0.1 o la IP del
# computador. Definirla solo si la API vive en otro servidor/puerto.
# VITE_API_URL=http://localhost:8000/api
