# Cada subproyecto tiene su propio .gitignore (backend/.gitignore, frontend/.gitignore).
# Este solo cubre lo que puede aparecer en cualquiera de los dos, o fuera de ambos.

.vscode/
.idea/
*.swp

.DS_Store
Thumbs.db
